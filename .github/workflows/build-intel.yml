name: Build Intel jamovi module

on:
  workflow_dispatch:
  push:
    branches:
      - "codex/intel-build-*"

jobs:
  build-intel:
    runs-on: macos-15-intel
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Intel R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.5.0"

      - name: Install jmvtools
        run: |
          Rscript -e 'install.packages("jmvtools", repos=c("https://repo.jamovi.org", "https://cloud.r-project.org"))'

      - name: Build and verify Intel module
        run: Rscript scripts/build_intel_jmo.R

      - name: Upload Intel module
        uses: actions/upload-artifact@v4
        with:
          name: pathj-intel
          path: pathj_*_intel.jmo
          if-no-files-found: error
